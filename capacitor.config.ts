import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CameraCordovaFileClobber',
  webDir: 'www',
  bundledWebRuntime: false,
  npmClient: 'npm'
};

export default config;
